CREATE TABLE AccelerometerDatum
(
	 X DOUBLE PRECISION,
	 Y DOUBLE PRECISION,
	 Z DOUBLE PRECISION,
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE AltitudeDatum
(
	 Altitude DOUBLE PRECISION,
	 Accuracy DOUBLE PRECISION,
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE AmbientTemperatureDatum
(
	 DegreesCelsius DOUBLE PRECISION,
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE BatteryDatum
(
	 BatteryLevel DOUBLE PRECISION,
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE BiologicalSexDatum
(
	 Sex VARCHAR(500),
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE BirthdateDatum
(
	 Birthdate TIMESTAMPTZ,
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE BloodTypeDatum
(
	 BloodType VARCHAR(500),
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE BluetoothDeviceProximityDatum
(
	 DeviceName VARCHAR(500),
	 Address VARCHAR(500),
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE CellTowerDatum
(
	 CellTower VARCHAR(500),
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE CompassDatum
(
	 Heading DOUBLE PRECISION,
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE Datum
(
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE FacebookDatum
(
	 AgeRange VARCHAR(500),
	 FirstName VARCHAR(500),
	 Gender VARCHAR(500),
	 UserId VARCHAR(500),
	 LastName VARCHAR(500),
	 TimelineLink VARCHAR(500),
	 Locale VARCHAR(500),
	 FullName VARCHAR(500),
	 Timezone VARCHAR(500),
	 UpdatedTime BIGINT,
	 Verified BIGINT,
	 Email VARCHAR(500),
	 Biography VARCHAR(500),
	 Hometown VARCHAR(500),
	 Location VARCHAR(500),
	 RelationshipStatus VARCHAR(500),
	 SignificantOther VARCHAR(500),
	 Religion VARCHAR(500),
	 Politics VARCHAR(500),
	 Website VARCHAR(500),
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE HeightDatum
(
	 HeightInches DOUBLE PRECISION,
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE ImpreciseDatum
(
	 Accuracy DOUBLE PRECISION,
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE InputCompletionRecord
(
	 Timestamp TIMESTAMPTZ,
	 CompletionValue BIGINT
);


CREATE TABLE LightDatum
(
	 Brightness DOUBLE PRECISION,
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE LocationDatum
(
	 Latitude DOUBLE PRECISION,
	 Longitude DOUBLE PRECISION,
	 Accuracy DOUBLE PRECISION,
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE PointOfInterestProximityDatum
(
	 PoiName VARCHAR(500),
	 PoiType VARCHAR(500),
	 PoiLatitude DOUBLE PRECISION,
	 PoiLongitude DOUBLE PRECISION,
	 DistanceToPoiMeters DOUBLE PRECISION,
	 TriggerDistanceMeters DOUBLE PRECISION,
	 TriggerDistanceDirection VARCHAR(500),
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE ScreenDatum
(
	 IsOn BOOLEAN,
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE ScriptDatum
(
	 ScriptId VARCHAR(500),
	 ScriptName VARCHAR(500),
	 GroupId VARCHAR(500),
	 InputId VARCHAR(500),
	 RunId VARCHAR(500),
	 ScriptResponseValue BIGINT,
	 TriggerDatumId VARCHAR(500),
	 Latitude BIGINT,
	 Longitude BIGINT,
	 PresentationTimestamp TIMESTAMPTZ,
	 LocationTimestamp BIGINT,
	 CompletionRecords BIGINT,
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE ScriptRunDatum
(
	 ScriptId VARCHAR(500),
	 ScriptName VARCHAR(500),
	 RunId VARCHAR(500),
	 TriggerDatumId VARCHAR(500),
	 Latitude BIGINT,
	 Longitude BIGINT,
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE SmsDatum
(
	 FromNumber VARCHAR(500),
	 ToNumber VARCHAR(500),
	 Message VARCHAR(500),
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE SoundDatum
(
	 Decibels DOUBLE PRECISION,
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE SpeedDatum
(
	 KPH DOUBLE PRECISION,
	 Accuracy DOUBLE PRECISION,
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE TelephonyDatum
(
	 CallDurationSeconds BIGINT,
	 State VARCHAR(500),
	 PhoneNumber VARCHAR(500),
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE WlanDatum
(
	 AccessPointBSSID VARCHAR(500),
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE ScriptResponseValue
(
	 ScriptId VARCHAR(500),
	 ScriptName VARCHAR(500),
	 GroupId VARCHAR(500),
	 InputId VARCHAR(500),
	 RunId VARCHAR(500),
	 Response VARCHAR(500)
);


CREATE TABLE ParticipationRewardDatum
(
	 Participation DOUBLE PRECISION,
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);


CREATE TABLE ProtocolReportDatum
(
	 Error VARCHAR(500),
	 Warning VARCHAR(500),
	 Misc VARCHAR(500),
	 OperatingSystem VARCHAR(500),
	 Id VARCHAR(500),
	 DeviceId VARCHAR(500),
	 Timestamp TIMESTAMPTZ,
	 HashCode BIGINT,
	 Anonymized BOOLEAN
);