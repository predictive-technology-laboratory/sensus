'use strict';

describe('myApp.LandingPage module', function() {

  beforeEach(module('myApp.LandingPage'));

  describe('LandingPage controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var LandingPageCtrl = $controller('LandingPageCtrl');
      expect(LandingPageCtrl).toBeDefined();
    }));

  });
});